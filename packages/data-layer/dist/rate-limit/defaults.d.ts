/**
 * Default rate limits per provider (can be overridden)
 */
export declare const DEFAULT_RATE_LIMITS: Record<string, {
    maxRequests: number;
    windowMs: number;
}>;
//# sourceMappingURL=defaults.d.ts.map